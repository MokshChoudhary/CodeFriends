#include <QtNetwork>

/**
 * @brief is_connected check for network connectivity
 *        if internet is connected to device it send <u>true</u>
 *        otherwise return <u>fales</u>
 * @return bool value
 */
bool is_connected(){
    /**
     * @todo Implemetnt internet connection test
    */
    return true;
}
